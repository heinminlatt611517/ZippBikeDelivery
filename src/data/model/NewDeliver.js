class NewDeliver {
  constructor(id, assigned_order_id, delivered_date, status) {
    this.id = id;
    this.assigned_order_id = assigned_order_id;
    this.delivered_date = delivered_date;
    this.status = status;
  }
}

export default NewDeliver;
